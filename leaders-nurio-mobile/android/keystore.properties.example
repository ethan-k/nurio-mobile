# Copy this file to keystore.properties and fill in Study Leader app values.
# DO NOT commit keystore.properties to version control.
# storeFile is resolved from the android/ Gradle root, so keep it relative to this file.

storeFile=leaders-nurio-release.jks
storePassword=your_store_password
keyAlias=your_key_alias
keyPassword=your_key_password
