// Copy to LocalOverrides.xcconfig (ignored by Git). Only Nurio Local includes it.
// Use your Mac's reachable LAN address and run Rails listening on 0.0.0.0.
NURIO_BASE_URL = http:/$()/your-mac.local:3000
